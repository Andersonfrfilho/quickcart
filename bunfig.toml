[install]
# Lockfile binário, como o repositório sempre usou. O bun 1.2+ grava texto por padrão, então um
# `rm bun.lockb` seguido de install troca o formato sem ninguém decidir.
saveTextLockfile = false
