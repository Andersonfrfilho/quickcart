{
  "name": "@quickcart/frontend-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'frontend-web ainda não implementado — chega na Fase 8' && exit 1",
    "typecheck": "tsc --noEmit",
    "build": "echo 'frontend-web ainda não implementado — chega na Fase 8' && exit 1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
