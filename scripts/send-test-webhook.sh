#!/usr/bin/env bash
# Monta um payload de webhook Meta assinado (HMAC de dev) e faz POST no webhook local.
# Implementado na Fase 3 (webhook + WhatsAppSender) — ver .specs/features/mvp/tasks.md T3.5.
set -euo pipefail

echo "⚠️  make test-msg ainda não está implementado (chega na Fase 3)." >&2
echo "    MSG=\"$1\" TEL=\"$2\"" >&2
exit 1
